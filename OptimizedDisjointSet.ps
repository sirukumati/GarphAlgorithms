class UnionFind:
	def __init__(self,size):
		self.root = []
		self.rank = []
                self.root = (value for i in range(0,size))
		self.rank = [1] * size
	
	def find(self,x):
		if x != self.root[x]:
 			self.root[x] = self.find(root[x])
 		return self.root[x]

	def union(self, x , y):
		rootX = self.find[x]
		rootY = self.find[y]
		
		if self.rootX != self.rootY:
			if self.rank[rootX] > self.rank[rootY]:
				self.root[rootY] = rootX
			elif self.rank[rootX] < self.rank[rootY]:
				self.root[rootX] = rootY
			else:
			     self.root[rootY] = rootX
			     self.rank[rootX] += 1
	
	def connected(self,x,y):
		return self.find[x] == self.find[y]
